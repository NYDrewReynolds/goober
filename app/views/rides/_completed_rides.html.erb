<div class="row">
    <div class="col-md-4 col-md-offset-4 text-center">
      <h3>Completed Rides</h3>
    </div>
</div>
<div class="row">
  <div class="col-md-12">
    <table class="table table-bordered table-striped">
      <tr>
        <th>Rider</th>
        <th>Driver</th>
        <th>Pickup Location</th>
        <th>Dropoff Location</th>
        <th>Requested Time</th>
        <th>Accepted Time</th>
        <th>Completed Time</th>
        <th>Ride Cost</th>
      </tr>
      <% completed_rides.each do |ride| %>
          <tr>
            <td><%= ride.ride_rider.name %></td>
            <td><%= ride.ride_driver.name %></td>
            <td><%= ride.pickup_location %></td>
            <td><%= ride.dropoff_location %></td>
            <td><%= time_tag ride.requested_time, :format => '%B %d, %Y %l:%M %p' %></td>
            <td><%= time_tag ride.accepted_time, :format => '%B %d, %Y %l:%M %p' %></td>
            <td><%= time_tag ride.dropoff_time, :format => '%B %d, %Y %l:%M %p' %></td>
            <td><%= ride.cost %></td>
          </tr>
      <% end %>
    </table>
  </div>

</div>
